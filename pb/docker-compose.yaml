services:
    pocketbase:
        image: "pocketbase:latest"
        restart: unless-stopped
        ports:
            - 8080:8080
        volumes:
            - "./pb_test:/pb/pb_data"  # Attaches pb_data to a volume named 'pocketbase-data' on your current directory
